{
  "name": "waitlist-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "list-emails": "wrangler kv:key list --binding WAITLIST_KV"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
